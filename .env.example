# Never commit your real secret. Testnet only, free XLM from Friendbot.
# Generate one with: npx tsx -e "import {Keypair} from '@stellar/stellar-sdk'; console.log(Keypair.random().secret())"
SECRET_KEY=SXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
